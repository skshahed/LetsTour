<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:background="@drawable/maps"
    android:gravity="center" >

    <!-- TODO: Update blank fragment layout -->
    <TextView
        android:id="@+id/nearByTitle"
        android:layout_width="match_parent"
        android:layout_margin="10dp"
        android:layout_height="wrap_content"
        android:lineSpacingExtra="3dp"
        android:paddingRight="10dp"
        android:paddingTop="10dp"
        android:paddingLeft="15dp"
        android:paddingBottom="15dp"
        android:gravity="left"
        android:layout_gravity="center"
        android:textColor="#0f6b87"
        android:textSize="16sp"
        android:fontFamily="sans-serif"
        />
    <ImageView
        android:id="@+id/showPlaceImage"
        android:layout_width="match_parent"
        android:layout_height="wrap_content" />

    <Button
        android:id="@+id/placePicker"
        android:layout_width="200dp"
        android:layout_height="wrap_content"
        android:text="Find Place"
        android:textAllCaps="false"
        android:background="@drawable/place_picker_button"
        android:textColor="#071f89"
        android:textSize="20sp"
        android:layout_gravity="center"
        android:layout_below="@id/nearByTitle"
        android:fontFamily="sans-serif"
        />


</LinearLayout>
